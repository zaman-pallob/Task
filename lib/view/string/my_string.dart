import 'package:flutter/material.dart';

class MyString extends StatelessWidget {
  const MyString({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
